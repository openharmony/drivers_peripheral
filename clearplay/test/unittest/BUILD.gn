import("//build/ohos.gni")
import("//build/test.gni")

ohos_unittest("clearplay_unittest") {
  include_dirs = [
    
  ]
  sources = [
    
  ]

  deps = [ 
    
  ]

  cflags = [
    
  ]

  external_deps = [
    
  ]

  install_enable = false
  install_images = [ chipset_base_dir ]
  subsystem_name = "hdf"
  part_name = "drivers_peripheral_clearplay"
}